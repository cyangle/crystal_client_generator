# {{#lambdaPrefixWithHash}}{{> api_info}}{{/lambdaPrefixWithHash}}

# Unit tests for {{moduleName}}::{{classname}}
# Automatically generated by openapi-generator (https://openapi-generator.tech)
# Please update as you see appropriate
{{#models}}
{{#model}}
require "../spec_helper"
require "../../src/{{{shardName}}}/models/{{classFilename}}"

describe {{moduleName}}::{{classname}} do
{{^oneOf}}
  describe "test an instance of {{classname}}" do
    it "should create an instance of {{classname}}" do
      # instance = {{moduleName}}::{{classname}}.new
      # (instance).should be_a({{moduleName}}::{{classname}})
    end
  end
{{#vars}}

  describe "test attribute '{{{name}}}'" do
    it "should work" do
      # instance = {{moduleName}}::{{classname}}.new
      {{#isEnum}}
      # assertion here. ref: https://crystal-lang.org/reference/guides/testing.html
      # validator = {{moduleName}}::{{classname}}::{{#lambda.uppercase}}enum_validator_for_{{{name}}}{{/lambda.uppercase}}
      # validator.allowable_values.each do |value|
      #   instance.{{name}} = value.as({{dataType}})
      # end
      {{/isEnum}}
      {{^isEnum}}
      # assertion here. ref: https://crystal-lang.org/reference/guides/testing.html
      {{/isEnum}}
    end
  end
{{/vars}}
{{/oneOf}}
end
{{/model}}
{{/models}}
