# {{#lambdaPrefixWithHash}}{{> api_info}}{{/lambdaPrefixWithHash}}
# OpenAPI Generator version: {{{generatorVersion}}}

# Dependencies
require "crest"
require "log"
require "json"
require "time"
require "uri"

# Project files
require "./ext/**"
require "./validators/**"
require "./{{shardName}}/configuration.cr"
require "./{{shardName}}/api_error.cr"
require "./{{shardName}}/api_client.cr"

module {{moduleName}}
  Log = ::Log.for("{{moduleName}}") # => Log for {{moduleName}} source

  VERSION = {{ `shards version #{__DIR__}`.chomp.stringify }}

  # Return the default `Configuration` object.
  def self.configure
    Configuration.default
  end

  # Customize default settings for the SDK using block.
  #
  # ```
  # {{moduleName}}.configure do |config|
  #   config.username = "xxx"
  #   config.password = "xxx"
  # end
  # ```
  def self.configure
    yield Configuration.default
  end
end
